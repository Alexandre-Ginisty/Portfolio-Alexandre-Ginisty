const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Commentez ces lignes en développement :
  // output: 'export',
  // trailingSlash: true,
  images: {
    unoptimized: true,
  },
};
